// Auth state is now managed entirely by NextAuth v5.
// This file is kept for backwards-compatibility imports only.
// Use `useSession()` from "next-auth/react" directly in components,
// or the `useAuth()` hook from "@/hooks/use-auth".
export {};
